import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    padding: 8,
    backgroundColor: 'white',
  },
  name: { fontSize: 14, lineHeight: 20 },
  email: { fontSize: 12, lineHeight: 18 },
});
