import React from 'react';

import { View } from 'react-native';

const ListSeparator: React.FC = () => {
  return <View style={{ height: 1, backgroundColor: 'gray', opacity: 0.25 }} />;
};

export default ListSeparator;
